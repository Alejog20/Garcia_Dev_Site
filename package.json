{
  "name": "garcia-dev-site",
  "private": true,
  "version": "1.0.0",
  "description": "Portfolio site for Alejandro García. Zero-build static site; devDependencies are test-only and never ship.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
